---
# yaml-language-server: $schema=https://schema.blue-build.org/recipe-v1.json

name: valhallalinux
description: immuatuble linux distro based on Universal Blue & bootc.

base-image: ghcr.io/blue-build/base-images/fedora-base
image-version: 44 

# you can include multiple instances of the same module
modules:
  - type: files
    files:
      - source: system
        destination: / # copies files/system/ into your image's root folder.

  - type: dnf
    repos:
      copr:
        - ashbuk/Hyprland-Fedora
        - avengemedia/danklinux
    install:
      packages:
      # terminal apps
        - foot
        - micro
        - fish
        - fastfetch
        - git
      # desktop exp
        - wayland-utils
        - hyprland
        - dms
        - dms-greeter
        - brightnessctl

  - type: default-flatpaks
    configurations:
      - notify: true # Send notification after install/uninstall is finished 
        scope: system
        install: 
        # we love app images
          - it.mijorus.gearlever
      - scope: user 

  - type: os-release
    properties:
      ID: valhallalinux
      ID_Like: fedora
      NAME: Valhalla Linux
      PRETTY_NAME: Valhalla Linux (Fedora Based)

  - type: signing # this sets up the proper policy & signing files for signed images to work fully
